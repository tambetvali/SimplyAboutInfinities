# Simply About Infinities

This is the second attempt for an article like this.

## Strict Goal: two base units of infinities.

To reach infinities, we arrive to two aspects, which allow us to calculate infinities.

In Game Theory, exponent function is known to grow faster than any linear function.

***Unit 1: logexp qualities of number scale.***

Linear number exists on linear axe, and as you add to it, grows linearly.

If you turn this axe itself, point after point, to exponent or logarithm number, in first case linear-inside number becomes exponent and in second case, it becomes logarithm, if space function and value function equal as in Number Alchemy, "as below so above".

Logarithm axe contains numbers I call "sub-zero", not by their size but measurement amplitude; exponential numbers I call "super-infinity" or "infinite".

Notice that units z (log), x (lin), y (exp) exist so that each number with given unit exists on it's own coordinate system. Now, as when you add, subtract etc in relation to this system, you do this in terms of original, linear function; result is always projected in system-relative scale.

Here, it becomes very important: both log and exp scales contain numbers, which escape linear system. If logic is reprojected two times, so that in simple symmetry to base logic where you can talk about ideals such as "good and bad", each relation repeated in each unit z, x, y, and you are interested in parallel results and their horizons: calculating horizons, you see how they become relative, and infinities can be compared.

***Unit 2: discrete infinities***

If your range of the system is always so:
- T = base range, for example 8
- R = exp range, for example 8

Or:

- T = log range
- R = base range

I call them lower and upper complex.

Now the number has 16 values:
- It grows from 1 to 8 at octave T, which are linear values.
- Then it grows from 9 to 16 at octave R, which are exponential values (R=1, number 9, contains all the range from ranges of 1 to 8, this means slice 0.0:8.0 if ordinar number is slice, and same type of amounts follow).

Now, when those numbers are operated, this forms the system:
- Values of R, when they are operated with operations, do space transitions relative to T transitions, but in new, extended space:
- Multiplication of two values, which have R = 2 / 8 and R = 3 / 8, gives (2 * 3) / (8 * 8)
- Addition of two values also yields the result to 8 * 8 space.
- Division and subtraction, in sense of density, do not remove the complexity above, but add complexity below: with these operations, number precision of discrete number raises by 64 (8 * 8) for result.
- Symmetric dimensionality reduction gives the end result in original format - only relevant bits are used in each case.

Now, these numbers:
- Yield true for every relation to infinity

## Other units, introduction

Relations which hold between different instances, for example if slices are seen identical, influence the result:

Give slices A and B, line segments 1 cm and 0.5 cm long.

Both contain infinite number of points and these infinities would be "uncomparable" with some things known - as distinct unit cm exists for comparison;
- The size of point, is unknown as size of infinity,
- but the operation which can be done with constituents, works on it as if in any given relation, their measure and symmetries must be *same for all members*, so altough point size is imaginary, this is not imaginary that they won't lose any existing relation, such as real comparison in cm scale.

What is the "point":

Infinitesimal might be point address, and most specifically one can show infinities of coordinate numbers, which can be addressed on ideal lines and segments: these coordinate numbers constitute abstract "points".

## End of introduction

After the part which was given, you should be able for basic understanding of critical theorems I made for actual measurements:
- Original text "Simply About Infinities" explained every paradox and solution in large space of them I was within;
- but two measures given in beginning of this document, including the "other units", by which I mean mathematical consistency of every indirect measurement, relation and operation, make you able to navigate relations in infinity in symmetric way - altough *infinity itself is not measurable in lower dimension*, relations given make it simulate a number:
  - altough number does not exist, but relations give you consistent results and imaginary number scales appear by defined units, keeping every relation:
    - It's like mocking you: altough no real result is there "how big is infinity in ordinary linear counting", every number and relation as you infinitely gather more and more, is still consistent if you go backwards to original numbers;
    - Such as adding 1 necessarily adds 1.

## Remember

Meanwhile, you can read:
- [Infinite Circle](https://github.com/tambetvali/Landscopes/blob/main/Landscapes/infinitecircle.md)
- [Exterior Space](https://github.com/tambetvali/Landscopes/blob/main/Landscapes/exterior.md)

These two texts are in "Landscopes/Landscapes" repository: the one which explains infinities, but using simple parallel frameworks which rather mean to make you travel and navigate in it's scopes, than trying to give a base set of axioms with proof: occasionally, a base set of axioms with proof is still what you see, because of the Zen: what you give away, you get; if you get a fine journey through infinite landscape, indeed the theorems and axioms paint themselves under your foot, into your Root.

### Sources

- [Spireason Infinities](https://spireason.neocities.org/#infinity)
- [LaeArve Infinities](https://github.com/tambetvali/LaeArve#introduction) - here it's just supposed to go on and on
- [Theorem Infinities](https://github.com/tambetvali/LaeSpiEssentialTheorems/tree/main/LaegnaTheorems/InfinityAndZero) - this is supposed to be bare bone, central and critical theorems

# CoPilot's part

With this introduction, CoPilot should get inspired: let him use his own understandings and mindset, to find the models, scopes, landscapes, theorems and proofs to get us through infinities.

