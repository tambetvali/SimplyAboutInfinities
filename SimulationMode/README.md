# Simulating Laegna infinity structures

We use the approach parallel to Laegna Deep Learning basic Algorithm here (a simple extension to existing DL to enable Laegna Learning, an AI capable to think in terms of applied and particular Laegna in efficient, optimized way, not just linguistically and with theoretical math parallels):

On Laegna website, you first click on "Scroll Down" at top of the page, which initially appears; then you see list of icons below greeting; intuitively, it includes ⌸ (calculator) icon so now click this to smoothly scroll to calculator area: Exponometer is the computerized automation of this primitive into the DL.

The simulation mode will be (or already is at time of reading if not changed) fifth member of https://spireason.neocities.org/#infinity section of Laegna Infinties and SpiReason. We build up a conceptual simplification parallel to Exponometer, but applied science in different field: theoretical simulation.

It will be contained here: https://github.com/tambetvali/SimplyAboutInfinities repository, "SimulationMode" to understand Laegna based on journey into infinity.

# Number Data files

Basic Laegna numbers, compatible for lower math:

[sheepcounter.json](sheepcounter.json) - https://spireason.neocities.org/Playground/SheepCounter1/sheepcounter.json

Basic and Waveform Laegna numbers, compatible for lower and higher math respectively:

[animation.json](animation.json) - https://spireason.neocities.org/Playground/InferenceCounter/animation.json

Laegna Octave numbers, compatible with zero and infinity math, and repeating patterns in different realms:

[octaves.json](octaves.json) - https://spireason.neocities.org/Playground/OctaveCounter/octaves.json

These files together contain the Laegna Number Database in original form, and generators given at main site [https://spireason.neocities.org/#sheep](https://spireason.neocities.org/#sheep) allow to see the animations where frame positions are given in first, and I think ordered position for each number in second and third file for example. At this website section, either user can animate this (json is rather for robots and AIs), or one can download generators: with generators, you can find single numbers to change, to generate numbers with more digits in all three systems, and achieve larger json files, possible extensions to Laegna Number Database. An AI could use the code files to confirm structure it learns from examples, and it's applicability to non-examplified numbers which come beyond.

Number Data files have linear, discrete, naturally ordered version of Laegna numbers, and static boundary positions:
- This is designed specifically for ordinal-level logic and math, and initial study, when more complex Laegna systems were already applied to it's domains of thinking and math.
- Typically, same boundary letters are used differently in each number system, but the version contained is somewhat reasonable for both signed and unsigned numbers.
- Typically, Laegna number is two-dimensional, but ordering given allows the simplest linear transformation to it's meanings accross theories, thus it's kind of "true" ordinal meaning.
- Educationally: Sheep Counter is intented to be parallel to learning numbers in each language, where you typically count Natural Numbers from zero or one, to some degree such as 1000, and if this is your high-alarm mode mentally prepared, you learn typical system in one such counting.
  - Educationally, Sheep Counter tries to give similar experience.
  - Json files contain information for animation, timing and numbers with their meanings. Animations, games, and browsers can be produced easily, even with one CoPilot session as single, not very large HTML file for basic criteria of truth and applicability: html, also, can use json file dynamically to avoid copying errors and dramatic sizes of intelligent data to handle (time and space) complexity.
- Computationally:
  - Because implementing whole theorem framework is very complex,
  - simple set of numbers can be used to emulate operations,
  - json files can thus be used directly or indirectly to produce basic data for introductionary math, used in game, emulation or creation of small sets of operation mappings, such as primitive table of multiplication - digitwise, from I to E, 16 cells are contained in complete table of multiplication, and thus you use R = {1, 2}, namely one- and two-digit length numbers to confirm each form and understand the symmetries and hidden implications such as inside, there might be *one or two digit number* or *only two digit numbers*, different symmetrically in some applications and their particular definitions.
 
Simulation Mode Laegna infinity basics thus contains:

- Three files, which are used for
  - a *single project* (most basic demonstration of journey to infinity)
  - *standard datasource*: three files are the most critical, and two of them can be read in 99% parallel based on R and T indexes of number positions.
    - sheep counter project on website contains many data files, generators and dynamic or static users of these files and contained data, so it's not the "simplest thing to learn".
  - *explained result of Laegna research*

For an AI:
- https://github.com/tambetvali/SimplyAboutInfinities could be link given to discuss, which could be contextual starting point on discussing infinities.
- You can use https://github.com/tambetvali/SimplyAboutInfinities/tree/main/SimulationMode as link to json structured data which is easy to use in RAG to seek patterns and align understanding of Laegna numbers with theory by an AI; it can be used as direct source, or database for generating various flashcards: either direct field-to-field correlation, such as "Number 100 in Laegna", or to create or generate math sets, such as alternative to "1 + 1 = 2" flashcard, but in Laegna numbers. Such AI is most probable to start mastering Laegna math, it's theory and applications with you - meanwhile, the one which knows this folder, pointed out what it's json files are and which potential they contain for AI in both it's RAG and training modes, or preparing further files; this is compressed, automated, ordered and symmetric form of things, which appear scattered and contain domain or special cases in most files about Laegna theory. Those domains and special cases, basically, are *mappings of this to discrete and continuous, finite and infinite spaces, through allowed transformations*, where typical transformation is *affine transformation in finite or infinite space and it's symmetries*, such as multiplying the whole system with single standard infinity, doing some operations of scale, then dividing back by same infinity, which in Laegna, in many cases leads to original result without remembering the *identity*, but only the *value* of given infinity in another number system (ZXY).

The magic illusion why numbers still appear under zero and infinity scales:
- Near and "below" zero, compared to it's immediate symmetries, very slight change of value is critical.
- Near and "above" infinity, every slight digital, rounding, approximation or analogous bias is not just resulting with butterfly effects and infinite chains and fractals, but it most probably *already is*, almost definitely if we use various criteria to choose a *balanced* number system, not the one where another number is always infinitesimal away.
- Therefore, scale symmetry is taken and conditions resolved:
  - Symmetric, local to scale linear system appears which *exactly duplicated original system*.
    - Proof: in not only value, but domain of number, such as Int8 (between 0 and 255), adding one value - such as 256 - changes *relative positions of number* so it cannot play it's repeated games in infinity to be called a "pattern", rather, every number which has scoping mistake that small, giving similar and not identic template in similar and not identic template space; naturally, domain error yields number error, which could be lost in local conditions, but not in repeated games, infinite pattern where even one infinitesimal appearing in every cycle modifies fractal and not introduces: *is* infinite error by this proof.
  - Every operation and every operational space must be identic in every scale, and identic in every scale_x => scale_(x+1) step where each step is symmetric to each other step.
  - Finite realm is symmetric to infinite: octaves in linear space are finite symmetry from music theory to octaves of infinite scale.
  - This happens because: number is seen two-dimensional, and both dimensions of symmetric growth; line of a digit is thus step-by-step from differential orders down to zero (minus octaves equal minus differential orders), zero (integral and differential zero equal) and plus (plus octaves equal integral orders). This number system is vertical, through scopes; it re-resolves to identical condition (local linearization by symmetry, where relative log, lin and exp are not induced to values, but their axes - relativity theory has physical boundaries to speed and relative position to original position, and this happens if only linear space is available - those "limits", are actual infinities not perhaps in every reality, but in mathematical models where infinities are particular purpose and not metaphysical fact or "truth": log->lin->exp growth can be modelled linearly if minus and plus infinity map to past limit of log and future limit of exp, and with radius where circle becomes linear, it's edge is straight line not curve: at this imaginary size (non-topological) the limit value, such as *exact* speed of light or *exact* boundary of your travellable space would be reached; we can show these are not reached, thus impossible in linear space, as well as topology becomes impossible *exactly at that point*, but it's consistent again, giving *acceleration phases*, once we zoom out the *coordinate system itself*, which means we *apply exponent to underlying coordinate distribution, which remains linear without this exponent, and each 4 basic math operations as applied directly to Z scale are used before*.
 
For example:
- Z100 + Z200 (classic interpretation: +0 * 100 + +0 * 200, where +0 is abstract, synchronized zero).
  - Zero approaches this theorem: for two infinitesimals X = +0 and Y = +0, class of X = class of Y for every instance of symmetric size; from class => instance, +0 = +0.
  - In Laegna theory, this theorem which is not applied to classic math, means we achieve coordinate system stability, use use class based calculations parallel to metasystem.
  - Class <-> Instance, for example Class Real and Instance of type Real, is setwise equal (sets of implementation) to Infinity <-> Identity, for example Infinity of Real and Identity Reals (single, self-identical and mutually different Reals which constitute the basic set of the class, where set does not contain repeated members).
    - By this Mathematic(Class <-> Instance) <---> Semantic(Infinity <-> Identity), from having same underlying sets and Mathematics => Syntax, from first is mathematical syntax is implied, thus while the second one might not have math, it *does have syntax* for Infinity if it has one for Identity, and thus it has math.

Now:

- Number system density is log, linear or exp in three dimensions: Z, X and Y.
  - Linear (real) growth exists from Z to X to Y, line which passes them all one after another linearly, altough the function is changing.
    - This is proven by how circle behaves if it's size is infinity, how it behaves towards other circles, and what is topology if we use three lines where each position has three positional digits with same information containity vertically, symmetrically, and for each digit in each direction. Diagonal directions are symmetric as well, by which the number becomes into circular space needed for acceleration into infinity: one step in diagonal space is one step in horizontal or vertical space, but one diagonal is accelerated to infinity (topleft and bottomright), other is frozen to zero (the one where two axes cancel each others). Thus, if 1-4 are used for IOAE, 1-8 is used if each number is one horizontal or diagonal step, from IOAE = 1234, but 5678 are diagonal numbers. This infinity of 18 reaches absolute, linear edges of infinity, while 14 - IOAE -, the base Laegna reaches fractal which can be zoomed, altough limit values for given values would also touch infinite repetitions of directions (such as A(a) - digit A, with infinity of equal digits "a" after comma, fix the limit value rather than slice of this digit).
  - Nonlinear (complex) growth exists, where instead of one linear line passing three spaces, each disting space becomes separate frequency and three-dimensional system is used instead, and this produces non-linear, complex view.
  - Those systems, because of properties of base-4 (S is single possible value of single digit range) system and systematic use of base-4-round (1, 2, 4, 8 etc.) numbers of digits (R is number of digits), can express either 3-dimensional symmetry through Z, X, Y; or 2-dimensional through (Z, X) or (X, Y).
  - We are going to introduce third method in this document: we simplify Z, X, Y into two-dimensional system which might have internal hidden states, but has digit-wise symmetric distribution of values towards infinity and zero, in terms of some interesting properties used in Exponometer.
  - More conversions exist - translations, which are defined, all allow to define basic units and coordinate systems.

# Physical resonance of Laegna

Laegna system might be defined in these terms:
- IE can point to log and exp scales
- OA then points to minus and positive linear scales

Changing inputs for + and - (linops) do not change positioning, neither does * and / (logexpops).
- Using mixed combinations of linops and logexpops changes ordering of functions when you scale and move the input.
- Number system, which reaches log, lin and exp, means that graphically: unless specific criteria is taken, such as Discrete Number Theorem which provides local linearity regards to given calculation for linear theorems and implied binary compability, but this definition where even four signs exist: *, +, -, /, such as not +100, but *100 is allowed if you want to be relativer to upper edge of infinity (it's rather covered in 1-8 scope and not used in standard, 1-4 math which involves fractal infinities).

If true and false are applied to Laegna scale as -infinity to +infinity, log and exp axes repeat this and results are both metaphysical (infinite pattern, a penetrating class of truth-value coordinates), they are identic and inside each axe of numbers, same symmetries repeat. By O-Notation, if Z or Y exists, X does not exist in infinity, thus but minus Z or plus Y are definitely overplaying, accelerating faster, thus smaller and larger values in infinity compared to locality. Faster or slower, infinite difference, yields difference of those ranges in terms of three, distinct parts of equal infinities.

It's also non-linear: three values are easily modified independently.
- It's linearly thinkable in abstract terms: exact same theory can be repeated for Z, X, Y. It's implied that it repeats in Z=>X=>Y as -1, 0, 1, a vertical ordering, because they are symmetric.
- Funny but think: identical linear or exponent growth is given in number 1, if scale-positional dependency is allowed.

We see all dimensions of Laegna:
- CoPilot once asked: why so many?
- Now we are starting to reach: because every, naturally, is implied by standard number and it's existing relations.
- Number systems are bent by metaphysic tensor fields to account in infinity, to not distort their initial bias of having slight infinitesimal dissymmetries yield infinite bias;
  - Altough when constructing Laegna number system, every bias becomes infinity;
  - Laegna system itself is stable, balanced set of constraints to watch exist, and biases in theories do not scale infinitely, because altough measurement can be impresice: the impresicion is precise.

 spireason.neocities.org/#sheep - third sheep counter demonstrates:
 - Laegna numbers are demonstrated in physical effect.
 - Only base-4 projection of light, or compatible, yields straight waveform;
 - others provide "lightwave inference", but it's actually non-order of continuous space:
   - wave is circular, but it's visible form is not.
   - this is physical effect,
     - it shows up in Laegna system and it's reasoning,
     - it shows up in physics,
     - not because physics is non-ideal,
     - but merely because it's linear, yet adaptible and correct in infinity.
     - this proves both physics through numbers (ideal, simple) and numbers through physics (demonstrable, simulable)

